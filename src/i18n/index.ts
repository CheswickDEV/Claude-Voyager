import { de } from "./de";import { en } from "./en";export type Locale = "de" | "en";const dictionaries: Record<Locale, typeof en> = {  de,  en};export type TranslationKey = keyof typeof en.popup;export const getPreferredLocale = (): Locale => {  if (typeof navigator === "undefined") {    return "en";  }  const [language] = navigator.language.split("-");  return language === "de" ? "de" : "en";};export const t = (key: TranslationKey, locale: Locale = getPreferredLocale()): string => {  return dictionaries[locale].popup[key];};